# personal library data — never commit a user's collection
collection.db
backups/
img_cache/
local_cards.json
localdb_meta.json
local_cards_*.json
localdb_meta_*.json
local_prices_*.json
riftbound_cards.csv

# generated TLS cert + marker
cert.pem
key.pem
.cert_ip

# compiled OCR helper (auto-built from ocr.swift on macOS)
ocr

# vendored/installed deps & caches
segno/
__pycache__/
*.pyc

# misc
.DS_Store
.claude/
*.log

# raw screen recordings & screenshots (compressed copies live in docs/)
mobile library page.gif
*.mov
Screenshot*

# downloaded precon decklist cache (rebuilt on demand by the Deck Builder)
precon_cache/
precon_index.json

# design handoff bundle (reference material for the v1.1.0 redesign)
design_handoff_library_redesign/
