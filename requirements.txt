# Everything runs on the Python 3.9+ standard library.
# Optional extras (the tracker runs without them):
segno            # phone-pairing QR code
# cactus-needle  # assistant bubble — pip install cactus-needle
